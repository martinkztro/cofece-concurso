---
console.log('hello world');

interface Props {
    title: string;
    description: string;
    image: string;
}

const { title, description, image } = Astro.props;
---

<div class="bg-cyan-100 flex flex-col items-center p-2">
    <div>
        <img src=`${image}` alt="Imagen Post">
    </div>

    <div class="p-2">
        <h1 class="font-black text-xl text-center" >{ title }</h1>
        <p class="text-left">{ description }</p>
    </div>

    <a class="py-1 px-4 bg-cyan-400 rounded-3xl text-white font-bold" href="\">Ver más</a>
</div>