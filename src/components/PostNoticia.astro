---
interface Props {
    title: string;
    description: string;
    image: string;
}

const { title, description, image } = Astro.props;
---

<div class="bg-[#00a099] rounded-xl flex flex-col items-center p-2">
    <div>
        <img src=`${image}` alt="Imagen Post">
    </div>

    <div class="p-2">
        <h1 class="font-black text-white text-xl text-center" >{ title }</h1>
        <p class="text-left text-white font-light">{ description }</p>
    </div>

    <a class="py-1 px-4 bg-[#ffffff] rounded-3xl text-black font-bold" href="\">Ver más</a>

</div>